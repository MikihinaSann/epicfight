package yesman.epicfight.api.utils;

@Deprecated
public abstract class TemporaryCode {
	// Fix attack damage attribute default becomes 2.0
	/**
	public static void fix_21_11_1_3(Player player) {
		if (player.getAttributeBaseValue(Attributes.ATTACK_DAMAGE) == Attributes.ATTACK_DAMAGE.value().getDefaultValue()) {
			player.getAttribute(Attributes.ATTACK_DAMAGE).setBaseValue(1.0D);
		}
	}
	**/
}
